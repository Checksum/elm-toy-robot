{
    "name": "@checksum/elm-toy-robot",
    "private": true,
    "scripts": {
        "start": "parcel index.html --no-source-maps",
        "prebuild": "rm -rf dist",
        "build": "parcel build index.html --no-source-maps"
    },
    "devDependencies": {
        "elm": "0.19.1-3",
        "elm-hot": "^1.1.4",
        "node-elm-compiler": "^5.0.4",
        "parcel": "^1.12.4"
    }
}
